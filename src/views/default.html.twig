<!DOCTYPE html>
<html>
	<head>
		<title>{{ page }}
			- App Store Commerce</title>
	</head>
	<body>
		<ul id="navigation">

			{% for item in navigation %}
				<li>
					<a href="{{ item.href }}">{{ item.caption }}</a>
				</li>
			{% endfor %}
		</ul>

		<h1>{{ page }}
			- App Store Commerce</h1>
		{{ a_variable }}
	</body>
</html>
